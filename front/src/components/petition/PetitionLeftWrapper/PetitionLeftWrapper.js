import React from "react";
import "./PetitionLeftWrapper.css";

const PetitionLeftWrapper = ({ children }) => (
  <div className="petition-left-wrapper">
    {children}
  </div>
);

export default PetitionLeftWrapper;
