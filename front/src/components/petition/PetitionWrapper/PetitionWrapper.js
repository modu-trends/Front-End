import React from "react";
import "./PetitionWrapper.css";

const PetitionWrapper = ({ children }) => (
  <div className="petition-wrapper">
    {children}
  </div>
);

export default PetitionWrapper;
