import React from "react";
import "./PetitionRightWrapper.css";

const PetitionRightWrapper = ({ children }) => (
  <div className="petition-right-wrapper">
    {children}
  </div>
);

export default PetitionRightWrapper;
